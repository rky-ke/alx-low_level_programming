#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char h;

	for (a = '0'; a <= '16' ; a++)
		putchar (d);
	for (h = 'a' ; h <= 'f' ; h++)
		putchar (h);
	putchar ('\n');
	return (0);
}
